# 일별 주가 수집 (market 도메인)

> 목표: EPS 축적물과 결합해 PER(주가 ÷ EPS)을 보기 위한 일별 종가 수집. 원본만 저장하고 PER 은 조회 시 계산 — 통계 후작업 원칙 동일.

## 소스 결정 (2026-08-07)

- **공공데이터포털 금융위원회_주식시세정보** (`GetStockSecuritiesInfoService/getStockPriceInfo`).
  - 날짜(basDt) 기준 **전 상장 종목 일괄** 조회 — 페이지 5,000행이면 **하루치 전 종목 = 1호출** (실측 2,872행 단일 페이지 확인). 일일 트래픽 한도는 행이 아니라 **호출 수** 기준(기본 10,000/일)이라, 일상 수집(7일 창)은 하루 7호출, 과거 백필도 하루 한도로 약 40년치(연 250거래일)까지 가능.
  - 인증은 서비스 키 1개(`STOCK_PRICE_API_KEY`, DART 패턴). 키는 포털이 준 **인코딩된 원본 그대로** 쿼리에 넣는다 — 재인코딩하면 미등록 키 오류.
  - 당일 종가는 익일 반영 — 비실시간 원칙상 무해.
- 토스증권 Open API(2026-05 출시)는 보류: 시세가 종목 단위 조회 중심이라 전 종목 배치엔 호출 수가 불리하고, 계좌·IP 등록·OAuth 관리 비용 대비 강점(실시간·주문·미국주식)을 지금 안 씀. **즐겨찾기 푸시(실시간 감지)·미국 주식 확장 단계에서 재검토.**

## 실측 탐침 (2026-08-07)

- 2026-08-06 기준 totalCount 2,872 (KOSPI/KOSDAQ/KONEX 포함).
- 필드: basDt, srtnCd(6자리), mrktCtg, clpr/mkp/hipr/lopr(종/시/고/저), trqu(거래량), 그 외 trPrc(거래대금)·mrktTotAmt(시총)·lstgStCnt(상장주식수)는 **미저장** — 필요 시 컬럼 추가 + 기간 재수집(upsert 멱등)으로 과거분 백필 가능.
- 휴일은 totalCount 0 + 빈 배열(정상 응답).
- **거래정지 종목은 시/고/저가 0** 으로 옴 — 해석 없이 그대로 저장(raw 원칙).

## 구조

- `daily_stock_prices`: (stock_code, trade_date) 유니크, 시/고/저/종가·거래량·시장구분·상장주식수·시가총액·collected_at.
  - 상장주식수·시가총액(V10, 2026-08-09 승격): NULL 허용 — 원천이 빈 값을 주는 행 대비. 일별 원본을 그대로 두고 계단(변화 이벤트)은 파생층에서 `LAG` 로 추출 — 변화만 저장하는 방식은 수집을 상태 의존적으로 만들고 정정 흡수·오류 가시성을 잃어 기각.
- 수집: `POST /internal/daily-stock-prices/collect?from=&to=` — 기본 최근 7일 창, 날짜별로 전 종목 upsert(날짜 단위 트랜잭션). 창이 겹쳐도 upsert 멱등. 휴일은 0건이라 자연 스킵.
- 스케줄: 매일 18:10 KST (`DailyStockPriceScheduler`) — 7일 창이라 익일 반영 지연·서버 다운을 자체 흡수.
- 포트: `DailyStockPriceSource`(원천 사실 record 반환) / `DailyStockPriceRepository`(upsert + 종목별 최신 조회).

## 실측 (2026-08-07)

- 7일 창 라이브: 거래일 5일 × 전 종목 = 14,360행.
- **5년 백필**: 2021-08-09 ~ 2026-08-06, 6개월 × 10구간 22분, **3,377,272행 / 거래일 1,221일**. 사전 추정(340만~360만 행) 적중.
- 용량 실측: 데이터 260MB + 인덱스 265MB = **525MB** (추정 550~700MB 하단).
- 스팟 체크: 삼성전자(005930) 1,221일 결손 0, 최근 종가·거래량 실세와 부합.
- 하루 2,600(2021)→2,872(2026)종목 — 상장 종목 증가 역사가 그대로 반영.

## 다음

1. ~~라이브 검증~~ / ~~5년 백필~~ 완료.
2. ~~PER 조회~~ 완료 — 컨센서스 응답에 최근 종가·연도별 forward PER (적자·시세 부재 시 null). 라이브 검증(2026-08-08): 현대건설 종가 112,600원 기준 2026E 23.27배 ~ 2028E 14.66배, 독립 수계산과 일치. 2024 적자 연도는 null 로 정상 생략.

## 상장주식수·시가총액 승격 실측 (2026-08-09)

- **탐침**: 과거 날짜 조회가 그 시점 값을 보존하는지 — 셀트리온 2023-01(140.8M) vs 2024-02(218.0M), KB금융 2022(415.8M) vs 2025(393.5M)로 확인. 이 성질이 무너졌으면 백필 무의미했음.
- **백필**: 6개월×10구간 재수집(구간당 ~3분), 337만 행 100% 채움. upsert 멱등이 기존 행에 새 컬럼만 채우는 경로로 동작 (통합 테스트 고정).
- **스팟 체크**: 셀트리온 합병 신주 상장 2024-01-12 +50.5% 정확히 포착, 직후 소각 -1.0% 분리 포착. KB금융 소각 6회(2022~2026) 각각의 효력일에 계단. 삼성전자 2025-03-06 -0.84%·2026-04-14 -1.24%. 전 종목 감소 이벤트 연간 분포 33→110→166→240→351→629(2026 은 8월까지) — 밸류업 시기 소각 증가 추세와 부합.
- **주의**: 계단 추출(LAG)은 연속 구간 위에서만 유효 — 백필 중간 상태에서는 공백 양끝이 가짜 계단 하나로 뭉침을 실측으로 확인.
- 고유 종목 3,220개 > 현재 상장 2,872개 — 차이는 기간 내 상장폐지분. 시계열이 생존 편향 없이 보존됨.
- 소비처: Shareholder Return 신호 (product-direction.md Phase 1 — 소각만 인정, 감자 오탐 구분 필요).
